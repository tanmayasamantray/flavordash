import { Button } from "./ui/button";

const MainNav = () => {
    return (
        <Button variant="ghost" className="font-bold text-xl cursor-pointer text-[#272D2F] hover:text-[#FE724C]">Log In</Button>
    );
};

export default MainNav;